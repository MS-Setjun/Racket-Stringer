services:
  racket-stringing:
    build: .
    image: racket-stringing-app:latest
    container_name: racket-stringing-app
    ports:
      - "8501:8501"
    volumes:
      - app-data:/app/data
    restart: unless-stopped

volumes:
  app-data:
